#include<bits/stdc++.h>
using namespace std; 
int main(){
    string str="xyzahgfjgfj";
    // string str1="abc";
    // cin>>str; //input string with out space
    // getline(cin,str);//to take input full sentence with space
    // str.erase(1,2);//to erase element string_name.erase(starting index, no of element erase);

    // cout<<str.find("x")<<endl;//to find element in string but it output the starting index of element
    // cout<<str1.compare(str);//to compare two strings
    // string s1="hii";
    // str.insert(3,s1); //or (3,"hii");to insert element in string 3=no of index where insert element, 
    // cout<<str.length()<<endl;
    cout<<str.substr(2)<<endl;//to print substring of string 2=starting index of substring 4=no of element to print 
    // sort(str.begin(), str.end());//to sort string
    // cout<<'A'-'a';



    
    
    return 0;
}
